{
  "name": "environments",
  "version": "1.0.0",
  "description": "My environment setup scripts",
  "scripts": {
    "build:arch": "docker build -t environment-arch -f Dockerfile-arch .",
    "build:ubuntu": "docker build -t environment-ubuntu -f Dockerfile-ubuntu .",
    "start:arch": "npm run build:arch && docker run -it --rm --name environment-arch environment-arch bash",
    "start:ubuntu": "npm run build:ubuntu && docker run -it --rm --name environment-ubuntu environment-ubuntu bash",
    "shellcheck": "shellcheck provision.sh software/**/*.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/markgaze/environments.git"
  },
  "author": "markgaze",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/markgaze/environments/issues"
  },
  "homepage": "https://github.com/markgaze/environments#readme",
  "devDependencies": {
    "husky": "^4.2.5",
    "shellcheck": "^0.4.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run shellcheck"
    }
  }
}
